part of 'prompt_block_card.dart';

class _VideoBlock extends StatelessWidget {
  const _VideoBlock();

  @override
  Widget build(BuildContext context) {
    final style = context.textTheme.p;
    return Column(
      children: [
        // TODO: Implement video player/picker UI
        Text('Video Block - Not yet implemented', style: style),
      ],
    );
  }
}
