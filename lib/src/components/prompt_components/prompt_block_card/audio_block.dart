part of 'prompt_block_card.dart';

class _AudioBlock extends StatelessWidget {
  const _AudioBlock();

  @override
  Widget build(BuildContext context) {
    final style = context.textTheme.p;
    return Column(
      children: [
        // TODO: Implement audio player/recorder UI
        Text('Audio Block - Not yet implemented', style: style),
      ],
    );
  }
}
